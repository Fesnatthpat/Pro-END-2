# AI Context & Development Log สำหรับ Pro-END 2

## 📌 บทนำ (Introduction)
เอกสารนี้ถูกสร้างขึ้นมาเพื่อให้ AI Assistant (เช่น ผม) ไว้อ่านเพื่อทำความเข้าใจบริบทของโปรเจกต์ โครงสร้าง สถาปัตยกรรม และรับทราบสถานะการพัฒนาล่าสุดได้ทันที โดยไม่ต้องไล่ดูโค้ดทุกไฟล์ใหม่ทั้งหมด

## 🛠️ เทคโนโลยีหลักและเครื่องมือ (Tech Stack)
- **Framework:** Nuxt 4 (`compatibilityVersion: 4`) โดยใช้โครงสร้างแบบใหม่ในโฟลเดอร์ `app/`
- **Styling:** TailwindCSS v4 (`@tailwindcss/vite`), Animate.css, Material Symbols Rounded
- **Database / ORM:** PostgreSQL + Prisma (`@prisma/client`)
- **State Management:** Pinia (`@pinia/nuxt`)
- **Authentication:** Custom JWT (`jsonwebtoken`, `bcryptjs`)
- **BaaS / Storage:** Supabase (`@nuxtjs/supabase`)
- **UI Components & UX:** ใช้ SweetAlert2 สำหรับแจ้งเตือน (Alerts), Tailwind แบบ Custom (ไม่ได้ผูกติดกับ Nuxt UI แล้ว)

## 📊 ระบบและฐานข้อมูลที่พัฒนาแล้ว (Completed Systems & Schema)
ระบบมี Database Schema ที่พร้อมใช้งานแล้ว ดังนี้:
1. **ระบบ User Roles:** มี 3 ตารางแยกกันคือ `Student`, `Teacher`, `Admin` 
2. **ระบบข้อมูลพื้นฐานผู้ใช้:** นักศึกษามีที่อยู่ และดึงข้อมูลจังหวัด/อำเภอ/ตำบล ผ่าน library `thai-data`
3. **ระบบ Project Lifecycle:**
   - ตาราง `Project` เก็บข้อมูลหลักของโครงงาน (Step 1-5) และเชื่อมโยงสมาชิก/อาจารย์
   - ตาราง `ProjectStepStatus` เก็บรายละเอียดสถานะแยกตามขั้นตอนย่อย (1-5) เพื่อดูว่าแอดมินคนไหนเป็นผู้อนุมัติ/ปฏิเสธ พร้อม Feedback
   - ตาราง `ProgressReport` เก็บรายงานความก้าวหน้า (อัปโหลดไฟล์/ลิงก์)
   - ตาราง `ExamSchedule` เก็บกำหนดการสอบต่างๆ
4. **ระบบ Project Activity Logs (Timeline):** ตาราง `ProjectActivity` จะเก็บบันทึกประวัติทุก action ที่สำคัญ (เช่น ส่งงาน, แอดมินคอมเมนต์, อนุมัติผ่าน) เพื่อนำไปโชว์เป็น Timeline

## 🚀 สิ่งที่พัฒนาเสร็จไปแล้ว (What has been developed)
1. **การออกแบบ Database และ Prisma Setup:** โครงสร้างข้อมูลทั้งหมดพร้อมใช้งานและ migrate เรียบร้อย
2. **การตั้งค่า Project:** ติดตั้ง Tailwind 4, Pinia, Supabase และตั้งค่า Nuxt.config พร้อมใช้งานทั้งหมด

## 🚧 สถานะปัจจุบัน (Current Focus)
- **ตอนนี้กำลังทำหน้าอะไร:** ระบบแผงควบคุมของ Admin (Admin Control Panel)
- **ไฟล์เป้าหมายปัจจุบัน:** `app/pages/admin/project/[id].vue` (หน้าแสดงรายละเอียดของโครงงานรายตัว)
- **เป้าหมายของงานตอนนี้:**
  - นำข้อมูลโปรเจกต์มาแสดงผลให้แอดมินดู
  - สร้างระบบให้แอดมินสามารถ **อนุมัติ (Approve)**, **ส่งกลับแก้ไข (Revision)**, หรือ **ปฏิเสธ (Reject)** ในแต่ละ Step (1-5) ได้
  - สร้างส่วนแสดงผล Project Timeline (ดึงจาก `ProjectActivity`) ให้แอดมินดูความเคลื่อนไหวทั้งหมด
  - จัดการแสดงผลให้สวยงามและมีการโต้ตอบที่ดี (Dynamic UX)

## 💡 แนวทางการทำงานสำหรับ AI ในโปรเจกต์นี้
- **App Structure:** ทุกอย่างที่เกี่ยวกับ Frontend จะอยู่ใน `app/` (เช่น `app/pages/`, `app/components/`)
- **API:** เก็บไว้ในโฟลเดอร์ `server/api/` เพื่อเป็น Backend เล็กๆ ในตัว
- **บันทึกกิจกรรม:** เมื่อมีการเปลี่ยนแปลงสถานะโครงงาน จะต้องสั่ง create ข้อมูลใน `ProjectActivity` ควบคู่ไปด้วยเสมอ เพื่อให้ Timeline สมบูรณ์
- **อัปเดตไฟล์นี้ทุกครั้ง (CRITICAL):** เมื่อมีการแก้ไขโค้ด พัฒนาฟีเจอร์ใหม่ หรือเปลี่ยนโฟกัส **"ต้อง"** มาอัปเดตไฟล์ `AI_README.md` นี้เสมอ โดยเพิ่มรายละเอียดการเปลี่ยนแปลงลงในส่วน **📝 บันทึกการแก้ไข (Update Logs)** ด้านล่าง พร้อมระบุ วัน/เดือน/ปี และเวลา ให้ชัดเจน!

---

## 📝 บันทึกการแก้ไข (Update Logs)
*ส่วนนี้ใช้สำหรับให้ AI จดบันทึกสิ่งที่ตัวเองเพิ่งทำไป หรือฟีเจอร์ที่เพิ่งพัฒนาเสร็จ เพื่อให้ AI ในแชทถัดไปสามารถอ่านและทำงานต่อได้ทันที*

- **[22/06/2026 03:03]** - สร้างไฟล์ `AI_README.md` เพื่อใช้เป็นศูนย์กลางเก็บ Context การทำงานของ AI อัปเดตข้อมูลโครงสร้างโปรเจกต์ และเพิ่มกฎให้ AI ต้องบันทึก Log การทำงานทุกครั้งที่มีการเปลี่ยนแปลง
- **[22/06/2026 03:08]** - เพิ่มคอมเมนต์ภาษาไทยอธิบายการทำงานของโค้ดในไฟล์ `server/api/teacher/project-detail.get.ts` และ `app/pages/admin/project/[id].vue` อย่างละเอียด เพื่อให้อ่านโค้ดเข้าใจง่ายขึ้น
- **[22/06/2026 03:16]** - 🚀 **[เริ่ม Phase 1]** ทำการเพิ่มคอมเมนต์อธิบายการทำงานของโค้ดในไฟล์ทั้งหมดของโฟลเดอร์ `server/utils/` (`prisma.ts`, `rateLimit.ts`) และ `server/middleware/` (`01.security.ts`, `security-headers.ts`, `auth.ts`) เสร็จสิ้นเรียบร้อยแล้ว
- **[22/06/2026 03:19]** - 🚀 **[สลับมาเริ่ม Phase 3 ฝั่ง Frontend]** เพิ่มคอมเมนต์อธิบายการทำงานของ Vue Composition API ในไฟล์ `app/layouts/default.vue` และ `app/pages/admin/exam-topic.vue` เรียบร้อยแล้ว
- **[28/06/2026 18:45]** - 🔔 เพิ่มตัวเลขแจ้งเตือน (Notification Counter) ในแท็บ "รอดำเนินการ" และ "ตารางนัดสอบ" ในหน้าจัดการคำร้องของ Admin (`app/pages/admin/exam-topic.vue` และ `app/pages/admin/exam-final.vue`) เรียบร้อยแล้ว
- **[28/06/2026 18:55]** - 🎨 ปรับปรุงหน้าต่างรายงานสรุปผล (`app/pages/admin/reports.vue`) ให้มีรูปแบบที่เป็นทางการและดูเรียบร้อยขึ้น (ลดการใช้สี Gradient สดใส, ปรับสไตล์ Table และ Card ให้เป็นแนว Dashboard มาตรฐาน)
- **[28/06/2026 19:20]** - ⚡️ แก้ไขปัญหาหน้าจอตารางสอบหน่วงและไม่อัปเดตทันทีเมื่อกดยืนยัน โดยเพิ่มสถานะ Loading และปรับลำดับการปิดหน้าต่างก่อนแจ้งเตือนสำเร็จ (`exam-topic.vue`, `exam-final.vue`)
- **[28/06/2026 19:30]** - 🎨 ปรับปรุง UI หน้าข้อมูลอาจารย์ (`app/pages/admin/teachers.vue`) ให้เป็น Clean Design ทิศทางเดียวกับหน้าข้อมูลนักศึกษา (`students.vue`) และปรับดีไซน์ของหน้าต่างเพิ่ม/แก้ไขอาจารย์ใหม่
- **[29/06/2026 10:04]** - 🐛 แก้ไขหน้าแบบฟอร์ม (CP1, CP2, และ `exam-request.vue`) ในส่วนของช่องกรอกอีเมลถูกตัดคำ (Truncated) เมื่อมีขนาดยาวเกินไป โดยทำการแยกบรรทัดช่องอีเมลใหม่เพื่อให้ใช้งานพื้นที่ความกว้างได้เกือบเต็มหน้ากระดาษ A4
- **[29/06/2026 11:10]** - 📝 เพิ่มคอมเมนต์อธิบายการทำงานภาษาไทยให้กับ 29 ไฟล์ที่เหลือในโปรเจกต์ (ฝั่ง Frontend 10 ไฟล์ และฝั่ง Backend 19 ไฟล์) เพื่อปรับปรุงโครงสร้างเอกสารประกอบการอธิบายโค้ดให้ครบถ้วนสมบูรณ์
